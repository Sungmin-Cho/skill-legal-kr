# CLAUDE.md — legal-kr 프로젝트

> 이 파일은 AI 협업 도구(Claude)가 프로젝트 컨텍스트를 빠르게 파악하기 위한 안내 문서입니다.

---

## 프로젝트 개요

**legal-kr**은 대한민국 법령·판례 데이터를 검색하여 근거 기반 법률 조언을 제공하는 Claude Code 스킬 프로젝트입니다.
세 가지 요소로 구성됩니다:

1. **`legal-kr` 스킬** (`.claude/skills/legal-kr/`) — 변호사 스타일의 7단계 법률 상담 워크플로우 + 검색 스크립트
2. **법령 데이터** (`legalize-kr/`) — 대한민국 법령 전체를 Git으로 관리, 각 개정이 실제 공포일자를 가진 commit
3. **판례 데이터** (`precedent-kr/`) — 대법원·하급심 판례 12만여 건

- **데이터 출처**: [국가법령정보센터 OpenAPI](https://open.law.go.kr)
- **웹사이트**: [legalize.kr](https://legalize.kr)
- **GitHub**: [legalize-kr/legalize-kr](https://github.com/legalize-kr/legalize-kr), [legalize-kr/precedent-kr](https://github.com/legalize-kr/precedent-kr)
- **라이선스**: 법령·판례 원문은 공공저작물, 스킬 코드와 저장소 구조/메타데이터는 MIT

---

## 저장소 구조

```
legal-kr/                         ← 워크스페이스 루트 (이 저장소)
├── CLAUDE.md                     ← 이 문서
├── README.md                     ← 프로젝트 설명서
├── .claude/
│   ├── settings.json             ← 프로젝트 공유 권한 설정 (스킬 스크립트 실행 허용)
│   └── skills/legal-kr/          ← 변호사 스킬
│       ├── SKILL.md              ← 스킬 정의 (7단계 워크플로우)
│       └── scripts/              ← search_law.py, search_precedent.py
├── tests/                        ← 검색 스크립트 단위 테스트 (pytest)
├── outputs/                      ← 분석 결과물 저장 디렉토리 (git 추적 제외)
├── legalize-kr/                  ← 법령 데이터 저장소 (별도 clone, git 추적 제외)
│   └── kr/                       ← 대한민국 법령 디렉토리 (3,000여 개)
│       ├── 민법/
│       │   ├── 법률.md
│       │   └── 시행령.md
│       ├── 근로기준법/
│       │   ├── 법률.md
│       │   ├── 시행령.md
│       │   └── 시행규칙.md
│       └── {법령명(띄어쓰기 제거)}/
│           └── {문서유형}.md
└── precedent-kr/                 ← 판례 데이터 저장소 (별도 clone, git 추적 제외)
    ├── metadata.json             ← 판례 메타데이터 (사건명·사건번호·선고일자·경로)
    ├── stats.json                ← 데이터 규모 통계
    └── {사건종류}/{법원급}/{사건번호}.md
```

법령은 `legalize-kr/kr/` 아래 3,000여 개의 디렉토리, 판례는 `precedent-kr/` 아래 12만여 건의 파일로 구성됩니다.
정확한 규모는 `precedent-kr/stats.json`과 `ls legalize-kr/kr | wc -l`로 확인합니다.

---

## 파일 명명 규칙

| 문서 유형 | 파일명 | 설명 |
|-----------|--------|------|
| 법률 | `법률.md` | 국회에서 제정·개정하는 법률 |
| 시행령 | `시행령.md` | 법률의 시행령 (부모 법률 있음) |
| 시행규칙 | `시행규칙.md` | 법률의 시행규칙 (부령) |
| 대통령령 | `대통령령.md` | 독립 대통령령 (규정, 직제 등 — 부모 법률 없음) |
| 대법원규칙 | `대법원규칙.md` | 대법원에서 제정하는 규칙 |
| 기타 부령 | `{부처명}령.md` | 총리령, 국가보훈부령 등 |

**디렉토리명**: 법령명에서 띄어쓰기를 제거 (예: `친일반민족행위자재산의국가귀속에관한특별법`)

**Unicode 정규화**: `·` (U+00B7) → `ㆍ` (U+318D) 변환 적용 (예: `10·27` → `10ㆍ27`)

---

## 법령 파일 구조 (YAML Frontmatter)

각 `.md` 파일은 YAML 메타데이터 + Markdown 본문으로 구성됩니다.

```yaml
---
제목: 민법
법령MST: 284415           # 법령 고유 식별자
법령ID: "001001"
법령구분: 법률             # 법률 | 대통령령 | 시행령 | 대법원규칙 등
법령구분코드: A0002
소관부처:
  - 법무부               # 복수 부처 가능 (YAML 리스트)
공포일자: 2024-01-02
공포번호: "19834"
시행일자: 2024-07-03
법령분야: 민사
상태: 시행               # 시행 | 폐지
출처: https://www.law.go.kr/법령/민법
---

# 민법

## 제1편 총칙
### 제1장 통칙

##### 제1조(법원)
민사에 관하여 법률에 규정이 없으면 관습법에 의하고...
```

### 주요 메타데이터 필드

| 필드 | 타입 | 설명 |
|------|------|------|
| `제목` | string | 법령명 (정규화 적용) |
| `원본제목` | string | API 원본 법령명 (정규화 결과와 다를 경우만) |
| `법령MST` | number | 법령 고유 식별자 |
| `법령ID` | string | 법령 분류 코드 |
| `법령구분` | string | 문서 종류 |
| `소관부처` | list | 담당 부처 (복수 가능) |
| `공포일자` | date | 공포일 (YYYY-MM-DD) |
| `공포번호` | string | 공포 번호 |
| `시행일자` | date | 시행일 (YYYY-MM-DD) |
| `법령분야` | string | 법령 분야 (민사, 행정, 노동 등) |
| `상태` | string | 시행 / 폐지 |
| `출처` | url | 국가법령정보센터 원문 URL |

---

## Git 커밋 구조

```
법률: 민법 (일부개정)

법령 전문: https://www.law.go.kr/법령/민법
제개정문: https://www.law.go.kr/법령/제개정문/민법/(12345,20260317)
신구법비교: https://www.law.go.kr/법령/신구법비교/민법

공포일자: 2026-03-17 | 공포번호: 12345
소관부처: 법무부
법령분야: 민사
법령MST: 284415
```

- 커밋 날짜 = 법령 공포일자 (author/committer date)
- Unix Epoch(1970-01-01) 이전 법령은 커밋 날짜가 1970-01-01로 고정 → 실제 공포일자는 `공포일자` 필드 참조

---

## 주요 활용 패턴

```bash
# 특정 법령 열람
cat legalize-kr/kr/민법/법률.md

# 법령 개정 이력 확인
git -C legalize-kr log -- kr/민법/

# 전체 법령에서 키워드 검색
grep -r "개인정보" legalize-kr/kr/

# 특정 날짜 기준 법령 상태 확인
git -C legalize-kr log --before="2025-01-01" -1 -- kr/민법/법률.md

# 현재 시행 중인 법령만 필터
grep -rl "상태: 시행" legalize-kr/kr/

# 스킬 검색 스크립트 (권장 — 구조화된 JSON 결과)
python3 .claude/skills/legal-kr/scripts/search_law.py --exact "민법" --articles "제750조"
python3 .claude/skills/legal-kr/scripts/search_law.py --exact "민법" --as-of 2020-06-01 --articles "제750조"  # 행위시법 조회
python3 .claude/skills/legal-kr/scripts/search_precedent.py --case "2024다268508"
```

---

## 관련 저장소 (에코시스템)

| 저장소 | 역할 |
|--------|------|
| `legalize-kr/legalize-kr` | 법령 데이터 (현재 저장소) |
| `legalize-kr/legalize-pipeline` | 법령 수집 · 변환 · 검증 파이프라인 |
| `legalize-kr/compiler` | OpenAPI 결과 → Git 포맷 컴파일러 |
| `legalize-kr/legalize-web` | 웹사이트 프론트엔드 |

---

## 주의사항

- 파이프라인 개선 시 전체 히스토리를 재구성하는 **force-push**가 발생할 수 있음
  → fork/참조 시 `git fetch --all && git reset --hard origin/main` 으로 재동기화 필요
- 법령 본문은 소프트웨어 코드가 아닌 **법령 텍스트 데이터**이므로 코드 수정보다는 내용 조회/분석이 주요 작업
- 법령 조문 참조 시 `출처` 필드의 국가법령정보센터 URL을 통해 원본 확인 권장

---

## 결과물 저장 규칙

**상담형·분석형 요청**(종합 법률 분석, 비교·검토 보고서, 프로젝트 분석 등)의 결과물은 **반드시 파일로 저장**합니다.
**단순 조회**(특정 조문 확인, 단건 판례 검색 등)는 대화 응답으로 충분하며, 사용자가 요청하면 저장합니다.
이 기준은 `legal-kr` 스킬 Step 7(출력)의 저장 규칙과 동일합니다.

### 저장 위치

모든 결과물은 워크스페이스 루트(`legal-kr/`) 아래 `outputs/` 디렉토리에 저장합니다:

```
legal-kr/
└── outputs/
    ├── 민법_분석_20260412.md
    ├── 근로기준법_요약_20260412.docx
    ├── 개인정보보호법_비교표_20260412.xlsx
    └── ...
```

### 파일명 규칙

```
{법령명}_{작업유형}_{날짜YYYYMMDD}.{확장자}
```

예시:
- `민법_조문분석_20260412.md`
- `근로기준법_개정이력_20260412.md`
- `개인정보보호법_요약보고서_20260412.docx`

### 결과물 형식 기준

- **기본 형식은 `.md`** — 모든 결과물은 특별한 요청이 없으면 Markdown 파일로 저장합니다.
- **사용자가 형식을 명시한 경우**에만 아래 형식으로 저장합니다:

| 사용자 요청 예시 | 저장 형식 |
|-----------------|-----------|
| "워드 파일로", "docx로" | `.docx` |
| "엑셀로", "표로 정리", "xlsx로" | `.xlsx` |
| "PPT로", "슬라이드로", "pptx로" | `.pptx` |
| "PDF로" | `.pdf` |

- 저장 후에는 반드시 파일 링크를 제공하여 사용자가 바로 열 수 있게 합니다.
- `outputs/` 디렉토리가 없으면 자동으로 생성합니다.

---

## 스킬 활용 가이드

> ⚠️ **필수 규칙**: 법률 관련 질문·상담·분석·검색 요청에는 **반드시 `legal-kr` 스킬을 먼저 호출한 후 진행**해야 합니다.
> 스킬을 거치지 않고 일반 지식만으로 법률 답변을 하는 것은 허용되지 않습니다.
> (법률과 무관한 프로젝트 관리·코드 작업에는 이 규칙이 적용되지 않습니다.)

### 스킬 매핑

| 요청 유형 | 사용할 스킬 |
|-----------|-------------|
| 법률 상담 · 법령 분석 · 조문 해설 · 판례 검색 · 법률 리서치 · 계약/분쟁 자문 | `legal-kr` (`.claude/skills/legal-kr/`) |
| 문서 형식 변환 (docx/xlsx/pptx/pdf) | 별도 스킬 불필요 — 위 결과물 형식 기준에 따라 저장 |

### 스킬 호출 원칙

1. **법률 질문·상담·분석 요청** → 먼저 `legal-kr` 스킬을 호출
   (7단계 워크플로우: 데이터 최신화 → 요청 분류·인터뷰 → 상황 분석 → 법령 검색 → 판례 검색 → 종합 분석 → 출력)
2. **스킬 실행 후** → legalize-kr / precedent-kr 저장소의 원문 파일을 직접 참조하여 조문·판례 근거 보강
3. **결과물 저장** → 위 결과물 저장 규칙에 따라 `outputs/`에 저장 후 링크 제공

법령 파일 경로는 `legalize-kr/kr/{법령명}/{문서유형}.md`,
판례 파일 경로는 `precedent-kr/{사건종류}/{법원급}/{사건번호}.md` 형식을 따릅니다.
